export interface NotFoundPageProps {
  title?: string;
  message?: string;
  buttonText?: string;
  homeUrl?: string;
}

export interface ErrorPageClasses {
  container: string;
  content: string;
  errorCode: string;
  title: string;
  message: string;
  homeButton: string;
}
